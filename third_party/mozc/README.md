# Mozc Runtime Placeholder

Place pinned Mozc runtime artifacts for local bridge integration under `runtime/`.

The runtime directory is intentionally ignored until a redistributable build artifact and its exact license/notice payload are selected. Keep the upstream license and notices with any runtime files that are packaged for release.
